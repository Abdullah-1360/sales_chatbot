# Server Configuration
PORT=4000
NODE_ENV=production
CORS_ORIGIN=http://localhost:5173

# Logging Configuration
# LOG_LEVEL options: ERROR, WARN, INFO, DEBUG
# ERROR: Only errors
# WARN: Errors and warnings
# INFO: Errors, warnings, and info (default)
# DEBUG: All logs including detailed debug information
LOG_LEVEL=INFO

# WHMCS API Configuration
WHMCS_URL=https://portal.hostbreak.com/includes/api.php
WHMCS_API_IDENTIFIER=your_api_identifier_here
WHMCS_API_SECRET=your_api_secret_here
WHMCS_CACHE_TTL=300

# WHMCS Product Group IDs (GIDs) - Automatically synced:
# GID 1  = cPanel Hosting
# GID 2  = Windows Reseller
# GID 6  = SSL Certificates (Other)
# GID 20 = WordPress Hosting
# GID 21 = Business Hosting (WooCommerce)
# GID 25 = Windows Hosting
# GID 26 = SSL Certificates
# GID 28 = cPanel Reseller Hosting

# MongoDB Configuration
MONGODB_URI=mongodb+srv://anonymous:anonymous@cluster0.zhfof.mongodb.net/?appName=Cluster0
USE_MONGODB=true

# Server Cache Configuration (MongoDB)
SERVER_CACHE_TTL_MINUTES=30
SERVER_FORCE_REFRESH_HOURS=24

# Sync Configuration
# Note: Set AUTO_SYNC_ON_STARTUP=false on shared hosting with limited memory
# to avoid WebAssembly memory errors. Run sync manually or via cron instead.
AUTO_SYNC_ON_STARTUP=true
SYNC_INTERVAL_HOURS=24

# Exchange Rate Configuration
# Leave empty to fetch live rates from API, or set a fixed rate (e.g., 282.66)
FIXED_EXCHANGE_RATE=
AUTO_REFRESH_STALE_PRICING=false

# Legacy fixture mode (deprecated - use MongoDB instead)
USE_FIXTURE=false

# VTiger CRM Configuration (optional)
VTIGER_URL=https://your-vtiger.com/webservice.php
VTIGER_USERNAME=username
VTIGER_ACCESS_KEY=access_key
VTIGER_DEFAULT_TYPE=Not Set

# WHM API Configuration
# Configure API keys for each server you want to manage
# Format: WHM_API_KEY_<SERVER_NAME>=your_api_key_here
WHM_USERNAME=root
WHM_VERIFY_SSL=false

# Example server API keys (replace with your actual keys):
# WHM_API_KEY_CP1=your_cp1_api_key_here
# WHM_API_KEY_PCP3=your_pcp3_api_key_here
# WHM_API_KEY_RCP2=your_rcp2_api_key_here

# CSF (ConfigServer Security & Firewall) Integration
# CSF integration uses the same WHM API keys as above
# CSF endpoints are accessed via WHM API on port 2087
# Server name is automatically determined from client credential resolution
